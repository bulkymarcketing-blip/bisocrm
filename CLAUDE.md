# Biso CRM — FRONTEND repo context (`index.html`)

> **Scope: the FRONTEND repo only** (`bulkymarcketing-blip/bisocrm`, GitHub Pages, `index.html`). The backend (Cloud Functions + Firebase rules) is a **separate repo with its own `CLAUDE.md`** — do not edit backend logic from here. This file is for Claude Code working on `index.html`.
>
> **Owner:** Claude Code on this repo; the UI-redesign browser chat proposes update blocks. **Each fact has one home** — frontend engineering lives here; product intent, business rules, and the roadmap live in the browser-chat `SKILL.md`; the contract the backend owns lives in the backend `CLAUDE.md`. Don't copy content between the files.

Single-file bridal-styling CRM for Kushan (Biso by Dinushi, Negombo, Sri Lanka). Owner directs product; **no coding background**; tests on **iPhone Chrome**.

- Live at **crm.bisobydinushi.com** (GitHub Pages). Deploys automatically on push to `main`.
- **One file, `index.html` (~7,300 lines).** Vanilla HTML/CSS/JS. **No React/Vue/build tools — the single file is deliberate. Do not introduce a framework or a build step.**
- Talks to: Firebase Realtime DB (REST, no SDK for data) + Firebase Auth + Cloud Functions (the separate backend repo).

## Hard conventions (FRONTEND — these are real iOS / runtime constraints)

- **HTML via string concatenation, NEVER nested template literals.** Nested template literals crash silently on **iOS Safari** (Kushan’s test device). Hard rule for `index.html`. *(This rule is frontend-only; it does NOT apply to the backend repo’s Node code.)*
- **No native dialogs** (`alert` / `confirm` / `prompt`) — use **`showConfirm`**; user-facing notifications via **`notif`**.
- **Wrap any multi-write save in `window._saving = true/false`** — a 15-second polling loop overwrites local state otherwise.
- **Red = act-now only** (overdue, errors, danger). Never use red for informational urgency — amber for time cues, neutral otherwise.
- Fonts: **Cinzel** (brand wordmark + quote/invoice/receipt documents ONLY) + **Inter** (everything else, incl. all screen/page/section titles, data and numbers). Navy / gold palette.

## Audit — MANDATORY before every change (any FAIL = hard stop, investigate)

- **CSS-only changes:** prove scope — every changed line sits inside the app `<style>` block; `{` / `}` counts unchanged; the file OUTSIDE the CSS is byte-identical (md5 the head region and the tail region before/after); the CSS class-set is unchanged.
- **JS render changes:** `node --check` on the file PLUS a **runtime sim** — mount the changed render function with a mocked DOM + mocked data, assert it doesn’t throw and returns sane HTML, then regression-diff the untouched functions. **No sim harness is in the repo** — build a small one for the touched screen and **commit it** so later work reuses it.
- Static check should include the overlapping-Firebase-paths rule.
- `docCSS` contains CSS braces — **mock it** in any brace-match/extraction audit or it breaks the extractor.
- After shipping: deploy is automatic on push to `main`; **test on iPhone in a Chrome Incognito tab** (bypasses the PWA service-worker cache).

## Frontend status — Cycles A–C (LIVE in the committed `index.html`)

- **Cycle A:** sequential document numbering + payment receipts. The **frontend mints** numbers via `mintDocNum(type)` against `/counters/{invoiceSeq|quoteSeq|receiptSeq}` (formats `BI-/BQ-/RC-2026-####`; seeds 1247 / 3061 / 1000; monotonic, never reset across years). The backend is instructed never to mint/reset/clobber these.
- **Cycle B:** the standalone quote builder was removed — quotes are built only inside the **Overview Form**. A **Messages** tab placeholder sits in the bottom nav — the slot the two-way WhatsApp inbox UI (#6, “Cycle E”) will fill; its backend is already shipped (contract below).
- **Cycle C:** quote/invoice/receipt visual redesign — premium template, ceremony-grouped line items + subtotals, payment schedule, logo in all three docs, “Rs” currency. Doc functions: `dMoney`, `docSectionFor`, `docSections`, `docTotals`, `docSchedule`, `docCSS`, `docLogo`, `docHeader`, `docFooter`, `docWrap`, `buildPDF`, `buildReceipt`.
- **Already shipped UX:** Pipeline mobile (collapsible stage lanes, per-card hero “next step” CTA, idle/rotting badge, stage-definition help); per-user “My Account” (display name → `/profiles/{uid}`, read-only role badge, password reauth + reset, relocated notification prefs).
- **Known bug (frontend):** the notification bell badge clears only when an individual item is tapped, not when the feed panel opens.

## Build sequence (frontend items)

Locked order **1 → 2 → 4 → 5 → (3/6 UI)**: #1 Pipeline ✅, #2 My Account ✅, **#4 Discounts + promo-code catalog (next)**, **#5 Line items / packages (parent + sub-lines) + installment schedule ✅** (shipped — additive layer on the existing quote/item shape; the event↔quote binding is provably unchanged; see the structure contract below), then the **#3/#6 WhatsApp inbox UI** (Cycle E) against the shipped backend.

## Quote / item / installment-schedule structure (FRONTEND OWNS this contract — Feature #5)

A quote/invoice keeps its shape: `{ id, date, expiry?, items, notes, convertedToInvoice?, sent?, advance?(inv), payments?(inv), schedule? }`. **`items` is a flat keyed map** `{ key → item }`. Feature #5 added two OPTIONAL item fields and one OPTIONAL doc field — **nothing existing was renamed or removed**:

- **Item** (unchanged core): `{ key, desc, qty, unit, disc(%), linkedTo?, custom?, auto?, orderDiscount?, discType?, discValue? }`. `cLine(it)=(qty||1)*(unit||0)*(1-(disc||0)/100)`; `qTot=Σ cLine`.
- **Package PARENT** (NEW, optional): `isPackage:true` + `pkgCeremony:'wedding'|'homecoming'|'goingAway'|'engagement'|''` (`''`/absent = "Additional Services"). It is a **$0 grouping header** (`qty:1, unit:0, disc:0`), so `cLine(parent)=0` and `qTot`/Finance/refunds are untouched. **`pkgCeremony` is deliberately NOT `linkedTo`** — packages never collide with the event-binding helpers (`findLinked`/`syncQuoteItems` key off `linkedTo`).
- **Package CHILD** (NEW, optional): a normal item plus `parentKey:<parent key>`. Carries the full item field set; package total = Σ children. A package is **pinned to ONE ceremony** — `docSectionFor` resolves a child via `parentKey` FIRST, so a child is locked to the parent's `pkgCeremony` everywhere (a child can never display under a ceremony different from its parent, even if it somehow carried its own `linkedTo`); children are positive priced lines (package/order discounts use the existing top-level discount mechanisms, not negative child lines). **Document rendering:** inside a ceremony group the package renders as a light, muted **name-only** sub-label (`.pkglabel`) with its children listed beneath (`.item.pkgsub`); there is **no per-package subtotal in the document** — the only subtotal shown is the per-ceremony one. (The Overview-Form editor does show a live package total as an editing aid.)
- **`schedule`** (NEW, optional, on the quote AND carried to the invoice): `{ mode:'custom', installments:[ { id, label, amount, due } ] }` where `due` = `{type:'confirmation'}` | `{type:'date', date:'YYYY-MM-DD'}` | `{type:'beforeWedding', days:N}`. **ONE schedule concept:** `docSchedule` renders the custom installments when `schedule.mode==='custom'`, otherwise the existing auto bespeaking+balance schedule (unchanged). Absent ⇒ auto. Invoices mark each installment Paid/Partial/Due/Overdue via a paid-waterfall.
- **Backward compatibility:** a legacy flat item (no `isPackage`/`parentKey`) and a doc with no `schedule` render byte-for-byte as before — proven in `sim/quoteItems.sim.js` (HEAD-vs-WORK identical HTML on legacy data). Consumers updated in the same build: `docSectionFor`, `docSections`, `docSchedule`, `docCSS`, `buildPDF`/`prevAdv`, `convInv`, the Overview-Form `buildQuoteSection` + new `qiAddPackage`/`qiAddSubLine`/`qiSetPkgCeremony`/`_qi*`/`ofSchedule*` helpers, `ofSave`/`openOverviewForm`. Untouched (regression-diffed byte-identical): `cLine`, `qTot`, `invPaid`, `findLinked`, `upsertLinked`, `removeLinked`, `syncQuoteItems`, `recomputeOrderDiscount`, `ofDiscItemKey`, `ofSubtotalExclDiscount`, `docTotals`, `buildReceipt`. Sim: `node sim/quoteItems.sim.js`.

> **CROSS-REPO (carry to the backend repo):** the backend **`getBrideQuote(token)`** reads this same `items` map and mirrors `docSections`/`docSchedule`. Before the **Bride Portal** goes live, the backend must add handling for `isPackage` / `pkgCeremony` / `parentKey` (group package children under the parent's ceremony, parent is a $0 header) and for the optional `schedule` (custom installments vs. the auto bespeaking+balance fallback), then re-verify its mirror against the structure above. **Do NOT launch the bride portal until that reconciliation is done.**

## Data model (paths the frontend reads / writes)

`/brides` (pipeline + confirmed clients; a bride’s quotes / invoices / payments live underneath), `/leads` + `/customers` (phone-matched mirrors written by `convInv`), `/leadintake` (incoming enquiries — see contract), `/roles` (**read-only here; never written from the app** — role lives here, not in `/profiles`), `/profiles/{uid}` (self-editable display name), `/settings` (Firebase-synced app settings), `/counters` (doc numbers), `/notifications/{uid}`, `/notificationPrefs/{uid}`, `/fcmTokens/{uid}`, and the WhatsApp paths `/messages/{normalizedPhone}` + `/conversations/{normalizedPhone}` (for the #6 UI).

## Contracts the frontend DEPENDS ON (the backend owns these — match them exactly)

- **`/leadintake/{id}`** (the backend / chatbot writes; the frontend reads it in `openOverviewForm`): `id` (**MUST equal the `/leadintake` key — it’s reused as the bride id and as the delete path**), `name`, `phone` (**digits incl. country code, NO leading `+`**), `email`, `source`, `notes`, `weddingDate`, `events`, plus `nextReminderAt`, `remindersStopped`, `contactAttempts`, `stage`, `intakeAt`, `createdAt`. Review & Accept opens the Overview Form prefilled → saving promotes to `/brides` and deletes the intake record; Dismiss deletes it.
- **`normPhone`** (must match the backend’s rule exactly; default country code 94): strip non-digits → if len ≥ 11 and not leading `0`, use as-is → leading `0` → replace the leading 0 with `94` → 9 digits → prepend `94`.
- **`/settings` keys** the frontend owns and the backend may read: `defaultNotes`, `logoUrl`, `defaultBespeaking`, `balanceDueDays` (default 14 — when a balance is due/overdue), `bankDetails`, plus WhatsApp flags `waBotEnabled`, `waReGreetAfterDays`, `waAutoGreeting`, and `brideGreeting`.
- **WhatsApp inbox (#6 UI)** reads the backend-persisted thread: `/messages/{normalizedPhone}/{msgId}` — message shape `{ id, dir:'in'|'out', kind:'text'|'image'|'template', body, mediaUrl, templateName, at (ISO), by, waMessageId, status }` — and conversation state `/conversations/{normalizedPhone}` (`mode:'bot'|'human'`, `takenOverBy`, etc.). The reply box is enabled only inside the 24h window (else templates) and calls the backend callable **`waReply`**. Conversations are keyed by **normalized phone, NOT brideId**.
- **Bride Portal** (`bride-portal.html`, separate page, demo-mode built): consumes the backend `getBrideQuote(token)` / `acceptBrideQuote(token)` contract. **Don’t wire the CRM share button until the page is hosted** (avoids 404 links).

## Open frontend items (pre-existing)

- `b.quotations` vs `b.quotes` path drift — quotes should write to **`b.quotes`** (canonical), not `b.quotations` (legacy). Not yet fixed.
- `rCustomers` filter — scope to brides with invoices only?
- `_saving` guards still needed on ~7 lower-risk async functions.

## Process

Built in **Claude Code on this repo**: plan-then-approve (propose the plan + the exact functions you’ll touch, then wait for approval — no surprise code), implement, run the full audit above, commit. Deploy is automatic on push to `main`. The browser chat handles design / mockups / decisions.

**Merge policy (owner's standing rule):** develop on the designated branch, push it, and then **HOLD at the branch stage and wait for the owner's explicit go-ahead before fast-forwarding into `main`** for anything touching the **documents** (quote/invoice/receipt), the **data/quote structure**, app **logic**, or anything involving a **judgment call**. Only **genuinely trivial mechanical fixes** (e.g. a print-margin value, a one-line CSS value) may be fast-forwarded to `main` directly. **When in doubt, hold.**

-----

## Redesign — frontend visual overhaul (productivity-app direction)

> Added from the frontend design chat (June 2026). **This file is the single home of the redesign spec** — the browser-chat `SKILL.md` keeps only the high-level direction and points here. Phases 4–8 are built in **Claude Code on this repo**.

### Direction

White-background modern productivity app — “warm minimalism” (Linear / Things / Notion calm, but warm, not clinical). Keep the navy + gold brand. End state: this look across the whole app.

### Status

- **Phase 1 — global tokens — DONE & LIVE.**
- **Phase 2 — labels (de-uppercased) — DONE & LIVE.**
- **Phase 3 — tags (tamed) — DONE & LIVE.**
- **Phase 4 — Today / Daily Brief (list-first rebuild + concept realign) — DONE.** Flat grouped lists sitting **directly on the canvas** (no cards), the **BISO brand mark** in the topbar on this screen, and **empty weekly sections hidden**. Phases 1–3 are CSS-only and already in the committed `index.html` — confirm with `grep -c -- --canvas index.html` (must be ≥ 1) before any further phase, so you build on them and don’t undo them.
- **Phase 5 — Pipeline (stage pills + lighter cards) — DONE.** Stages-only (action widget dropped); lanes → a single-line `.spath` stage flow-path of `.fpill` pills with chevrons (`window._pipeStage`/`setPipeStage`, default first stage, active scrolled into view); `STAGES[2]/[3]` → "Visit Booked"/"Visited" (display-only); lighter flat cards with a navy CTA + inline SVG icons. See the phases list + `sim/pipeline.sim.js`.
- **Phase 6 — Schedule (filter pills + flat cards + scoped recolour) — DONE.** Boxed segmented control → `.fpill` row; Cinzel/uppercase headers → Inter sentence-case; appointment + follow-up cards flattened to `--surf`+1px+`--sh` (dropped amber fill / colour left-bar / dashed border); ink/navy action chips (no green); slim legend; Wedding→rose `#C77D8E`, Final Trial→wine `#8E2A4A` (scoped to `rSchedule`). See the phases list + `sim/schedule.sim.js`.
- **Phase 7 — Data screens — IN PROGRESS: Quotations + Profiles + Invoices DONE; Finance + Analytics remaining.** Reuse the Phase-4 `_brief*` vocabulary + the Phase-5/6 `.fpill` pill + flat-card patterns (see below).
- **Next: Finance.**
- **CONFIRMED page is DROPPED** from the redesign and from nav — Schedule is the chronological home for committed weddings, Profiles is the client directory, and the bride detail (`openDetail`) holds the full record.

### The token system AS SHIPPED — do NOT revert

In `:root` inside the app `<style>` at the top of `index.html`:

- `--canvas:#F7F5F0` — warm-white PAGE background (set on `body`). Tunable toward whiter as screens become flat lists.
- `--bg:#F4F2EC` — warm INSET FILL for inputs / table headers / list rows (this is NOT the page background anymore).
- `--surf:#FFF` — cards.
- `--border:#EAE7E0` — warm hairline. **All borders are now 1px** (the old 1.5px is gone).
- `--sh:0 1px 2px rgba(27,43,75,0.05)` — near-flat card shadow. `--sh2` stays heavier and is for overlays / dropdowns / modals only.
- `.sv` (stat numbers) → Inter + `font-variant-numeric:tabular-nums` (NOT Cinzel).
- `.btn-gold` → navy text on gold (not white).
- **Labels:** `text-transform:uppercase` removed from all functional labels (`th`, `label`, `.stitle`, `.dl`, `.nl`, `.qcll`, `.mc-lbl`, `.tab`, `.sname`, `.nav-sec`); letter-spacing ~`.2px`. **Still uppercase ON PURPOSE — do not touch:** `.auth-title`, `.auth-field label`, `.auth-brand .tg`, `.user-chip .rl`, `.av-menu .av-info .rl`.
- **Tags:** source pills neutral `#F0EEE8` bg / `#6C6E72` text, EXCEPT `.tw` (WhatsApp) green `#E7F2E9` / `#46815B`. `.tq` and `.twg` softened to amber (`#F5ECD6` / `#8A6D1E`). `.tu` urgent RED kept.

### Design rules (apply to every screen rebuild)

- **One gold, one job:** gold = primary actions + the active state ONLY (the `+`, primary CTAs, active tab/filter). Navy ink = text and state. “Ink for state, gold for action.”
- Separation via hairlines + whitespace, not heavy borders/shadows. Don’t introduce new shadows beyond `--sh`; overlays use `--sh2`.
- Sentence-case labels everywhere (except the documented brand/role flourishes above).
- Numbers in Inter `tabular-nums`. **Cinzel is reserved for the brand wordmark + the quote/invoice/receipt documents ONLY** — screen/page/section titles, data and numbers are all Inter. (Both the in-content "Today" heading and the shared topbar `.page-title` are now Inter bold/tight — no Cinzel-on-a-title holdouts remain.)
- Tags restrained: neutral default, WhatsApp green, amber for time cues, RED only for genuine act-now (overdue / error / danger) — never for informational urgency.
- Line icons (Tabler-style) in place of emoji where feasible.
- ~8px spacing rhythm, modest radii (10–16px).
- **iOS Safari: string concatenation only, NO template literals** (existing hard rule, still applies).

### Remaining phases — one audited cycle each, plan-then-approve, ship-to-test each

1. **Today / Daily Brief — DONE.** Flat grouped lists (sentence-case section header + muted tabular count), clean tappable rows (no fake checkbox — there's no persistent task store), appointment rows with cool event-type dots, an Inter "Today" heading + one-line "N things need you today" summary, Brief-scoped name search with a "No matches in today's view" empty state, demoted the metric-y *Since last visit* digest below the action lists. The global topbar `+ Add` is now gold (one gold, one job). **Concept realign (shipped on top):** lists are now FLAT on the warm-white canvas (no `--surf` cards/shadows), the topbar shows the **BISO brand mark** on this screen instead of a page title, empty "This week's weddings/appointments" sections are hidden, and the row quick-actions were lightened to calm `.bicon`/`.bact` (still navy).
1. **Pipeline — DONE.** Presentation + view-state only (leads still group by numeric `stage`; CTA actions unchanged). The pastel collapsible lanes were retired (`.pipeline`/`.stage-col`/`.shdr`/`.sbdy`, `SC[]`/`.p0–.p4`, `toggleLane`/`toggleLaneHelp`/`STAGE_DEFS`/`lane-help*` all removed) and replaced with a **stage-pill row** that **reuses `.fpill`/`.fpill.on`** (gold active, like the Brief): one pill per stage with its count, **default-selects the first stage**; the stage row is a single-line horizontal **stage flow-path** — a pipeline-only **`.spath`** `overflow-x:auto;flex-wrap:nowrap` scroller (the Brief's `.fpills` is untouched and still wraps) with a muted inline-SVG chevron (**`_icoSep`**, `var(--muted)`, non-interactive) between consecutive stages; active stage held in **`window._pipeStage`** (session; default 0) via **`setPipeStage(i)`**, the active pill (`id="pipe-pill-on"`) is scrolled into view so a bit of the next pill peeks, and the selected stage's leads render as a **flat stack of `lCard`s**. The Pipeline is now **stages-only** — the `renderTodaysActions` action widget was dropped from `rPipeline` (it's the Brief's job; `renderTodaysActions` itself is unchanged apart from the relabel below). Page title **"Lead Pipeline" → "Pipeline"**. **Stage rename (display-only, stage is a number):** `STAGES[2]` "Consultation Booked" → **"Visit Booked"**, `STAGES[3]` "Consultation Done" → **"Visited"**, with the card CTA "Mark consult done" → **"Mark visited"** and the Brief Quotes-to-send copy ("Consultation Xd ago" → "Visited Xd ago", meta "consultation done" → "visited"). **PENDING separate task:** the full consultation→visit wording sweep (the appointment reason `'Consultation'`, reminders, help/guide text) is intentionally NOT done yet. **Card treatment:** flat `--surf` + 1px hairline, **no red `lc-overdue` bar** (overdue shows via badges), **navy, compact inline `.lcard-cta-full`** next-step button (sized to content, `gap:5px`; `.lcard-cta-full svg{margin-right:0}` so only the gap spaces icon↔label; short labels per stage: Contact / Follow up / Mark visited / Create quote / Convert), quiet borderless `.lcard-icon-btn`, **inline SVG line icons (no Tabler CDN)** — emoji removed everywhere incl. the expanded view; tamed tag row (idle/age → quiet amber `.lidle` clock text not a red pill; reminder badge red only when Overdue/Today, amber Tomorrow, neutral later; Quoted/Wedding-in → neutral `.lchip`; attempt-count moved to the expanded view) + a muted **"Rs X quoted"** `.lamt` line on quoted cards. Closed-lost restyled (1px, sentence-case "Closed–lost"). **Sims:** `node sim/pipeline.sim.js` (pills/cards/widget-gone/rename + regression that `computeTodaysActions`/`_brief*`/`pipelineMetrics` are byte-identical and the Brief is unchanged except the relabel); `dailyBrief.sim.js`'s `[I]` proof is now relabel-aware.
1. **Schedule — DONE.** Presentation only, all inside `rSchedule` + its local helpers (`eventCard`/`section`/the old `modeBtn`+`toggleBar`/`legend`/local `typeMeta`). No data/logic touched: `setSchedMode` + `window._schedMode`, the event collection, and `openResched`/`markApptDone`/`openDisp`/`openDetail` are byte-identical. **Filter:** boxed segmented control → a **`.fpill`/`.fpill.on`** row (gold active), reusing the Today/Pipeline pill classes; `setSchedMode('appts'|'followups'|'both')` unchanged. **Headers:** Cinzel/uppercase → Inter sentence-case ("This week"/"This month"/"Later"), muted sub + right-aligned tabular count, 1px hairline. **Cards** (appointment AND follow-up) flattened to `var(--surf)` + 1px `var(--border)` + `var(--sh)` (Pipeline style) — dropped the amber fill, the coloured 8px left-bar, and the dashed follow-up border; leading cool event-type dot, navy name, muted `tabular-nums` "date · time", de-uppercased type tag, neutral "No-show" tag, neutral day badge (red ONLY for act-now follow-up overdue; never gold); all emoji removed. **Action chips** are ink/navy only — NO semantic colour on buttons: Reschedule = navy-filled, "Did come" = navy ghost, "Log attempt" = navy-filled. **Legend** slimmed: bordered `var(--bg)` box → light muted single-line wrap of dots + labels. **Scoped event-type recolour (coding only):** Wedding `#C8A55B` → **`#C77D8E`** (rose, tag bg `#F7E8EC`) to get the headline event off the reserved brand gold; Final Trial `#DC2626` → **`#8E2A4A`** (wine, tag bg `#F2E1E8`) to get it off the act-now red; the other six types untouched. The colour lives in **two hardcoded copies** — the local `typeMeta` map AND the `legendItems` array — which must stay in sync; the app-wide `.tu`/`.bu` urgent-red classes are **not** touched (recolour scoped to those two `rSchedule` literals + the `legendItems` copy). **Known minor:** Wedding (rose) sits close to Engagement (mauve `#B0506A`) as small dots — an intentional warm "ceremony-milestone" family separated by lightness; revisit Wedding's hue if it reads poorly. **Sim:** `node sim/schedule.sim.js` (pills / flat cards / "No-show" not "NO-SHOW" / recolour present + old hexes absent / slim legend / navy chips + regression that `computeTodaysActions`/`_brief*`/`rDailyBrief`/`renderTodaysActions`/`rPipeline`/`lCard`/`cardCTA`/`setSchedMode` are byte-identical vs HEAD).
1. **Data screens — Quotations + Profiles + Invoices DONE; Finance + Analytics remaining.** (Cycle A built the invoice/finance data + PDFs, NOT the screen visuals — so every data screen is in this redesign lane. Confirmed is dropped — see Status.)
   - **Profiles — DONE.** `rCustomers` rebuilt as the warm-flat people-directory (Lead/Client split). Presentation + one session filter; data/logic untouched — `cA`/`openCust`/`openNC`/`openDetail` byte-identical (regression-proven), and **`_briefRow` byte-identical** (the avatar uses a NEW `_briefRowLead` helper). **Removed:** the `<table>`/thead/tbody, the `.card`/`.card-hdr` + "Customer Profiles" title, the in-card "+ New" (the gold topbar `+` already does New lead on this view). Shared `.badge`/table CSS untouched. **New CSS — exactly two classes:** `.ini` (36px circular initials avatar, `#EDEAE2` bg / navy initials) and `.ts` (Client success tag, `#E6F2EB`/`#2F7A52`) — the class-set diff confirms only these two were added. **New JS:** `_initials(name)`; `_briefRowLead(leadHtml, mainHtml, rightHtml, onclick)` — a leading-element variant of `_briefRow` (with `_briefRow` left byte-identical); `setCustFilter(f)` + `window._custFilter` (default `'all'`, session-only, resets on reload, mirrors `setQuoteFilter`/`setBriefFilter`). **Pills:** a `.fpills` row of `.fpill`/`.fpill.on` (gold active) — All / Leads / Clients; `isClient` = has any invoice; filter also honours the topbar search (name|phone). **Rows:** `_briefSection('Directory',{count})` + `_briefList` + `_briefRowLead` — initials avatar + name + phone; right = Client (`tag ts`) / Lead (`tag ti`). Empty: "No profiles yet" / "No clients" / "No leads". **Deferred (engineering):** source tags on rows (the bride→customer view carries no source field — add source to that mapping to enable WhatsApp/Referral/Instagram tags); the quote/invoice counts intentionally moved off the row into the bride detail (`openCust`). **Sim:** `sim/profiles.sim.js` (no table/card/"+ New"; avatar rows; `tag ts`/`tag ti`; pills + the three filter sets; `_briefRow` byte-identical; exactly `.ini`+`.ts` added via class-set diff). NOTE: `rCustomers` was removed from `sim/quotations.sim.js`'s "every other `r*`" regression list (Profiles now legitimately changes `rCustomers`); `profiles.sim` is the authority for `rCustomers` going forward.
   - **Invoices — DONE.** `rInvoices` rebuilt to the warm-flat redesign. Presentation + one session filter; data/logic untouched — the brides→invoices collection, the date-desc sort, the per-invoice math (cancelled/tot/paid/bal/pct), `qTot`, `invPaid`, `invInvoiced`, `invCollected`, `invOutstanding`, `openIDoc`, and the `_brief*` helpers (incl. `_briefRow`/`_briefRowLead`) are byte-identical (regression-proven). **No new CSS classes.** **Removed:** the `.card`/`.card-hdr` + "Invoices" title/count, the `.lr*` markup, the "LKR ….00" money, the inline CANCELLED badge, the inline status-text + "Bal LKR …" line. `.lr*`/`.badge*` CSS left intact. **New JS:** `setInvFilter(f)` + `window._invFilter` (default `'all'`, session-only, resets on reload, mirrors `setQuoteFilter`). **Pills:** a `.fpills` row of `.fpill`/`.fpill.on` (gold active) — All / Unpaid / Partial / Paid (NO Overdue — deferred); status derived from `pct` + `cancelled` (math unchanged); cancelled appears only under All; topbar search honoured. **Rows:** `_briefSection('Invoices',{count})` + `_briefList` + `_briefRow` with a leading status dot — Paid `var(--success)` + `tag ts`, Partial `var(--warn)` + `tag twg`, Unpaid `var(--muted)` + `tag ti`, Cancelled `var(--muted)` + `tag ti`. NO red anywhere (red stays act-now only). `sub` = "id · date" (+ " · Rs X due" when bal>0); right = "Rs" total over the tag. Money "Rs" comma-integer, no LKR/decimals. Empty: "No invoices yet" / "Nothing under <pill>". **Deferred (engineering):** the Overdue status + Overdue pill + red dot (needs a balance>0 + past-due-date derivation). **Sim:** `sim/invoices.sim.js` (no card/.lr/LKR/.00/CANCELLED-badge; status-dot rows; `tag ts`/`twg`/`ti`; no `var(--danger)`/red; pills + filter sets; cancelled-only-under-All). NOTE: `rInvoices` was removed from the `quotations`/`profiles` "other `r*`" regression lists (`invoices.sim` is the authority); `profiles.sim`'s class-set check was made merge-robust.
   - **Quotations — DONE.** Presentation + one session filter only; data/logic untouched — the brides-read + exclude-converted collection (`if(!qt.convertedToInvoice)`), the date-desc sort, `qTot`, `openQDoc`, `markSent`, `moreQ` are byte-identical (regression-proven). **Removed:** the `.card`/`.card-hdr` wrapper, the Cinzel "QUOTATIONS" title, the in-card "+ New" (the gold topbar `+` already opens the quote form on this view), the `.lr*` markup, the "LKR ….00" money, the `.badge bo/bs` status badges. The `.lr*`/`.badge*` CSS is left intact (Invoices still uses it); no new CSS classes. **Built with the Phase-4 vocabulary:** a `.fpills` row of `.fpill`/`.fpill.on` (gold active) — All / Draft / Sent — driven by `setQuoteFilter(f)` + `window._quoteFilter` (default `'all'`, session-only, resets on reload, mirrors `setBriefFilter`/`setPipeStage`). Rows via `_briefSection('Quotes',{count})` + `_briefList` + `_briefRow` with NO leading dot; main = name + "id · date"; right = a right-aligned stack of "Rs " comma-integer (no LKR/decimals) over a status tag — Draft → `tag ti` (neutral), Sent → `tag twg` (amber). Empty: "No open quotations" (none) / "Nothing under <pill>" (filtered empty). **Deferred (data/feature — engineering chat, not this restyle):** the "Sent · 3d" age cue (sent is a boolean, no `sentAt`) and the Accepted / Converted pills+tags (Accepted needs the bride-portal data; Converted quotes are excluded by design). **Sim:** `sim/quotations.sim.js` (no card/.lr/LKR/.00/+New; the `_brief*` vocabulary; pills; `tag ti`/`tag twg`; the three filter sets; converted never appears) + regression byte-diff of `qTot`/`openQDoc`/`markSent`/`moreQ`/`_brief*` and every other `r*` render fn vs HEAD. (`sim/pipeline.sim.js`'s mocked `dU` was also made deterministic — sim-only; `rPipeline`/`lCard` byte-identical to HEAD.)
1. **Overview Form / bride detail & modals** — the remaining bespoke surfaces.
1. **Login decision + final polish & consistency sweep.**

### Phase 4 — component vocabulary (introduced in `rDailyBrief`, REUSE in Phases 5–6)

Presentation-only rebuild — every data source/action was preserved (the compute helpers `computeTodaysActions` and `_brief*` were untouched). The reusable list primitives, all in the app `<script>` just above `rDailyBrief`, are the shared vocabulary for the next screens:

- `_briefSection(title, {count, meta}, bodyHtml)` — sentence-case Inter section header + muted tabular count + optional right-aligned meta, then the body.
- `_briefList(rows)` — **flat list on the canvas**: emits the rows directly, separated by a 1px `--border` hairline (last row none). **No `--surf` fill, no border box, no radius, no shadow** (the concept's "flat grouped lists, not cards"). Reused by every Brief section.
- `_briefRow(dotColor, mainHtml, rightHtml, onclick)` — clean tappable row (`padding:12px 2px`, sits flush under its section header): leading dot + main + trailing quick-actions. `_briefMain(name, sub)` builds the two-line label; `_briefDot(color)` the dot; `_briefEmpty(text)` is a **plain muted line** (not a boxed card).
- `_apptTypeDot(reason)` — COOL, non-gold/non-red event-type dot (consultation blue `#5B7FA6`, trial teal `#3E8E8A`, pickup slate `#7A6E8E`, else `--muted`).
- `_briefMatchesQ(name)` — Brief-scoped name filter against the global `q` (the topbar search). Real cross-record global search is a separate later capability.
- `setPT(v)` — sets the shared topbar title element (`#PT`). On `dailyBrief` it renders the **BISO brand mark** (`textContent='BISO'`, class `.brand-mark` — Inter, `letter-spacing:4px`, navy); on every other view it uses `TITLES[v]` with class `.page-title`. **Routed through `sv()` and `render()` (incl. boot)** — all `#PT` writes must go through `setPT` so the brand-mark/title swap and the boot title both stay correct.
- **Calm Brief-only action classes:** `.bicon` (borderless icon button, ~40px tap target, grey glyph) for WhatsApp/Call, and `.bact` (small navy **ghost** chip) for Log / Create quote / Open. Navy only — gold stays the topbar `+`. **Pipeline's `.lcard-icon-btn`/`.lcard-cta-btn` are a SEPARATE set and were left untouched** — don't restyle those globally; add screen-scoped classes instead.
- **Hide-empty pattern:** render a whole section only when its `count > 0` (no empty-state card), e.g. "This week's weddings/appointments". Reuse this for sparse Phase-5/6 sections.
- **Filter pills (Daily Brief only):** a row of four pills — **All / Leads / Quotes / Visits** — sits after the "N things need you today" line (CSS `.fpills`/`.fpill`/`.fpill.on`; inactive = muted ghost, **active = gold** `.fpill.on` navy-on-gold, the sanctioned active-state gold that coexists with the topbar `+`). Active filter is a **session var `window._briefFilter`** (default `'all'`; persists across in-app navigation, resets to All on page reload — no storage); `setBriefFilter(f)` sets it and re-renders `rDailyBrief`. **Section→pill mapping** (a section shows when `f==='all'` or `f===` its pill): **Leads** → Follow-ups / New enquiries / Needs attention; **Quotes** → Quotes to send; **Visits** → Appointments to confirm / This week's weddings / This week's appointments; **All-only (no pill)** → Payments / Approval requests / Since last visit. Empty sections stay hidden; the topbar search still filters rows within a shown section; the "N things" count stays the unfiltered total. Empty filter ⇒ a quiet `_briefEmpty('Nothing under <Pill> today')` line.
- **`renderTodaysActions(actions, filter)`** now takes an **optional `filter` (defaults `'all'`)** that gates its four sub-groups (Follow-ups→`leads`, Appointments to confirm→`visits`, Quotes to send→`quotes`, Payments→`all`-only). The **Daily Brief passes the active pill**; **Pipeline (`rPipeline`) passes nothing → `'all'` → output byte-identical to before** (the shared widget is unaffected — proven in the sim). Don't make Pipeline pass a filter.
- Brief row CTAs stay **navy** (the calm `.bact`/`.bicon` above) so the only gold on the Brief is the topbar `+`. **Flag for Phase 5:** Pipeline cards still use gold WhatsApp CTAs (`btn-gold`), so with the gold `+` there are competing golds on Pipeline until Phase 5 harmonizes them.
- **Runtime sim lives at `sim/dailyBrief.sim.js`** — extracts render fns live from `index.html` (regex/string/comment-aware brace matcher), mounts them on a mocked DOM/data across 9 scenarios (incl. flat-list, brand-mark/`setPT`, calm `.bicon`/`.bact`, filter pills, and a byte-identical Pipeline-widget proof), and regression-diffs every untouched function vs `git HEAD`. Run `node sim/dailyBrief.sim.js` (exit 0 = pass). Extend it for later phases.

### Audit for the redesign — MANDATORY, same discipline as the rest of the project

- **CSS-only changes** (as in Phases 1–3): prove scope — every changed line sits inside the app `<style>` block; `{` / `}` counts unchanged; the file OUTSIDE the CSS is byte-identical (md5 the head region and the tail region before/after); the CSS class-set is unchanged. No native dialogs / template literals involved.
- **JS render changes** (Phases 4–8): `node --check` on the file PLUS a **runtime sim** — mount the changed render function with a mocked DOM and mocked `fbP`/data, assert it does not throw and returns sane HTML, and regression-diff the untouched functions. **There is NO sim harness in the repo** (the laptop sims were never committed), so build a small sim file for the touched screen — and **commit it** so later phases reuse it. Any FAIL = hard stop.
- After each phase: commit → deploy (GitHub Pages auto-deploys on push to `main`) → test on iPhone in a Chrome **Incognito** tab (bypasses the PWA service-worker cache).

### Process note (changed from the old split)

The frontend redesign is now built in **Claude Code on the connected repo**, not via browser-file-handover. The browser chat is used for design / mockups / decisions; Code implements + audits + commits. This supersedes the older “frontend via browser-Claude, file handed over” note for the redesign work.
